// Re-export from the main AuthContext to avoid duplicate implementations
export { AuthContext, AuthProvider, useAuth } from '../contexts/AuthContext';
export type { AuthContextType } from '../contexts/AuthContext';
